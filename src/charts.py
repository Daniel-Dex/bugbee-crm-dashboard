"""Chart helpers placeholder.

The first production version uses editable PowerPoint KPI/table primitives.
Add chart-specific builders here as soon as the exact API fields for channel,
category, product and funnel sources are validated in the live Magazord payload.
"""
